<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Cache directory -->
    <cache-path name="shared_images" path="shared_images/"/>

    <external-files-path name="external_app_files" path="."/>



    <!-- Internal files directory -->
    <files-path name="shared_files" path="."/>
    <files-path name="restaurant_bills" path="restaurant_bills/"/>

    <!-- External files directory (recommended for FileProvider) -->
    <external-files-path name="external_files" path="."/>
    <external-files-path name="restaurant_bills_external" path="restaurant_bills/"/>

    <!-- External cache directory -->
    <external-cache-path name="external_cache" path="."/>

    <!-- External storage (use with caution on Android 10+) -->
    <external-path name="external_storage" path="."/>
</paths>